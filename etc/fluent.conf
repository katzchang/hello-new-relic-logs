<source>
  @type tail
  @id hello
  path /fluentd/log/hello.log
  pos_file /fluentd/log/hello.log.pos
  tag hello
  <parse>
    @type none
  </parse>
</source>

<match **>
	@type newrelic
	@id newrelic_logs
	license_key "#{ENV['NEW_RELIC_LICENSE_KEY']}"
</match>
